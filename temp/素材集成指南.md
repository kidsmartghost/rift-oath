# 素材集成指南

## ✅ 已完成

### 1. 素材需求清单
- 📄 `/temp/素材需求清单.md` - 完整的素材需求文档
- 📄 `/temp/AI 提示词库.md` - 所有角色和场景的 AI 生成提示词

### 2. 素材配置模块
- 📄 `/src/data/assets.js` - 素材路径配置和辅助函数
  - 角色立绘配置（9 个角色）
  - 场景背景配置（15 个场景）
  - BGM 配置（9 首音乐）
  - 音效配置（10 个音效）
  - 场景到背景/角色的映射

### 3. AudioManager 扩展
- 📄 `/src/engine/AudioManager.js` - 已添加：
  - `playBGM()` - 播放背景音乐
  - `stopBGM()` - 停止 BGM
  - `fadeInBGM()` - 淡入 BGM
  - `fadeOutBGM()` - 淡出 BGM
  - `playSFX()` - 播放外部音效
  - `preloadSFX()` - 预加载音效

### 4. Game.vue 更新
- 已添加素材导入
- 已添加背景和立绘计算属性
- 已添加角色位置计算函数

---

## 🔧 待完成步骤

### 步骤 1：创建目录结构

```bash
cd /home/admin/openclaw/workspace/rift-oath

# 创建素材目录
mkdir -p public/images/characters
mkdir -p public/images/backgrounds
mkdir -p public/audio/bgm
mkdir -p public/audio/sfx
```

### 步骤 2：生成 AI 素材

使用 `/temp/AI 提示词库.md` 中的提示词：

#### 角色立绘（优先级：高）
1. **艾莉娅** - 使用 Midjourney/Leonardo
   ```
   anime style female knight, holy paladin, long silver blonde hair...
   ```
2. **K-7** - 赛博朋克机器人
3. **洛恩** - 半神天使
4. **林小满** - 现代高中生
5. **NPC 们** - 老人、Eve-9、米迦勒等

#### 场景背景（优先级：高）
1. **圣罗兰教堂** - 哥特式破败教堂
2. **废墟街道** - 赛博朋克废墟
3. **破碎神殿** - 古希腊风格
4. **普通教室** - 现代教室
5. **纯白空间** - 虚无背景

#### BGM 音乐（优先级：中）
1. **标题主题** - Suno/AIVA
2. **世界 1-5** - 各世界主题
3. **战斗/情感/结局** - 特殊场景

#### 音效（优先级：低）
可以用 Web Audio API 生成基础音效，或从 freesound.org 下载

### 步骤 3：保存素材文件

按照以下命名规范：

```
public/images/characters/
  alya_stand.png
  k7_stand.png
  lohn_stand.png
  xiaoman_stand.png
  oldman.png
  eve9.png
  michael.png
  observer.png
  creator.png

public/images/backgrounds/
  church.png
  wasteland_street.png
  bunker.png
  temple_ruins.png
  classroom.png
  library.png
  void.png
  ...

public/audio/bgm/
  title.mp3
  world1.mp3
  world2.mp3
  world3.mp3
  world4.mp3
  world5.mp3
  battle.mp3
  emotional.mp3
  climax.mp3
  ending.mp3

public/audio/sfx/
  click.wav
  select.wav
  typing.wav
  transition.wav
  unlock.wav
  ...
```

### 步骤 4：更新 Game.vue 的 loadScene 函数

找到 `loadScene` 函数，修改为：

```javascript
function loadScene(sceneId) {
  const scene = gameScript.scenes.find(s => s.id === sceneId)
  if (!scene) {
    console.error('场景不存在:', sceneId)
    return
  }
  
  currentScene.value = scene
  hasChoices.value = !!scene.choices
  
  // 加载背景
  const bg = getBackgroundForScene(sceneId)
  currentBackground.value = bg?.image || null
  
  // 加载角色
  currentCharacters.value = getCharactersForScene(sceneId)
  
  // 播放 BGM
  if (scene.worldId) {
    const bgm = getBGMForWorld(scene.worldId)
    if (bgm) {
      audioManager.fadeInBGM(bgm, 1500)
    }
  }
  
  // 检查结局
  if (scene.type === 'ending') {
    if (scene.unlockConditions && !checkEndingConditions(scene.unlockConditions)) {
      loadScene('ending_lost')
      return
    }
    
    setTimeout(() => {
      audioManager.playEnding()
      audioManager.fadeOutBGM(500)
      setTimeout(() => {
        audioManager.playBGM(assets.bgm.ending)
      }, 600)
      gameStore.unlockEnding(scene.id)
      gameStore.checkUnlocks()
      if (scene.title?.includes('第') || scene.title?.includes('结局')) {
        alert(`${scene.title}！感谢游玩！`)
      }
    }, 500)
  }
  
  const dialogueText = getDynamicDialogue(scene)
  startDialogue(dialogueText)
}
```

### 步骤 5：添加 CSS 样式

在 Game.vue 的 `<style scoped>` 中添加：

```css
/* 场景背景图 */
.background-image {
  position: absolute;
  top: 0;
  left: 0;
  width: 100%;
  height: 100%;
  background-size: cover;
  background-position: center;
  transition: background-image 1s ease;
}

/* 角色立绘层 */
.character-layer {
  position: absolute;
  bottom: 0;
  left: 0;
  width: 100%;
  height: 100%;
  pointer-events: none;
  display: flex;
  align-items: flex-end;
  justify-content: center;
  z-index: 30;
}

.character-portrait {
  position: absolute;
  bottom: 15vh;
  width: 200px;
  height: 300px;
  background-size: contain;
  background-position: center bottom;
  background-repeat: no-repeat;
  transform: translateX(-50%);
  transition: all 0.5s ease;
  filter: drop-shadow(0 0 20px rgba(0,0,0,0.5));
}

.character-name-tag {
  position: absolute;
  bottom: -30px;
  left: 50%;
  transform: translateX(-50%);
  background: rgba(0,0,0,0.7);
  color: #fff;
  padding: 4px 12px;
  border-radius: 12px;
  font-size: 12px;
  white-space: nowrap;
  border: 1px solid rgba(255,255,255,0.2);
}

/* 移动端优化 */
@media (max-width: 768px) {
  .character-portrait {
    width: 140px;
    height: 210px;
  }
}
```

### 步骤 6：测试

1. 访问游戏
2. 打开浏览器控制台
3. 检查是否有素材加载错误
4. 确认 BGM 播放正常
5. 确认立绘显示位置正确

---

## 🎨 快速测试（使用占位图）

如果暂时没有 AI 生成的素材，可以用占位图测试：

```bash
# 创建简单占位图（使用在线工具或代码生成）
# 例如：https://via.placeholder.com/200x300/8b0000/ffffff?text=Alya
```

或者在 assets.js 中临时使用颜色占位：

```javascript
// 临时测试用
portrait: 'data:image/svg+xml,...' // 或使用纯色
```

---

## 📊 工作量估算

| 任务 | 时间 | 工具 |
|------|------|------|
| 生成 9 个角色立绘 | 2-3 小时 | Midjourney/Leonardo |
| 生成 15 个场景背景 | 3-4 小时 | 同上 |
| 生成 9 首 BGM | 2-3 小时 | Suno/AIVA |
| 下载/生成 10 个音效 | 1 小时 | freesound/ElevenLabs |
| 后期处理（去背/调色） | 2 小时 | Photoshop/GIMP |
| 代码集成测试 | 1-2 小时 | - |
| **总计** | **11-15 小时** | - |

---

## 🚀 开始行动

准备好开始了吗？建议按以下顺序：

1. ✅ **今天**：生成 4 个主要角色立绘
2. ✅ **明天**：生成 5 个关键场景背景
3. ✅ **后天**：生成 3 首主要 BGM（标题 +2 个世界）
4. ✅ **第 4 天**：集成测试
5. ✅ **第 5 天**：补充剩余素材

需要我帮你做什么？
- 生成更详细的 AI 提示词？
- 编写批量下载脚本？
- 创建素材管理 UI？
